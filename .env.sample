# Arris Router Configuration Sample
# Copy this file to .env and fill in your actual values

ROUTER_URL=http://10.0.0.1
ROUTER_USER=your_username_here
ROUTER_PASS=your_password_here

# Optional: Specify cache directory if needed (defaults to /app/cache in Docker, src/ locally)
# SESSION_CACHE_DIR=/path/to/your/cache